import 'package:flutter/material.dart';

class GLeasingScreen extends StatelessWidget {
  const GLeasingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Leasing Pages"),
    );
  }
}